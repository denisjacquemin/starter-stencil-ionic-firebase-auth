{
  "name": "starter-stencil-ionic-firebase-auth",
  "private": true,
  "version": "0.0.1",
  "description": "starter-stencil-ionic-firebase-auth",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.0.0",
    "@stencil/core": "^1.8.8"
  },
  "dependencies": {
    "@capacitor/cli": "^1.5.0",
    "@capacitor/core": "^1.5.0",
    "firebase": "^7.9.1"
  }
}
